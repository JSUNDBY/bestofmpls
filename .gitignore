node_modules/
dist/
.DS_Store
.env
.env.local
*.log
.vscode/
.idea/

# Search Console service-account key (never commit)
.gsc-key.json
gsc-service-account.json

# Wrangler local dev cache
worker/.wrangler/
.wrangler/

# Generated weekly content plan
social-plan.md
growth/newsletter-preview.html
src/data/livingbest.json

# Shorts renderer scratch
.shorts-work/

# Prospect data stays OFF the public repo (businesses are named as weak)
growth/PITCH-LIST.md
growth/pitch-list.json
growth/outbox/
.tm-api-key
.dev.vars
.youtube-client.json
.youtube-token.json
growth/featured-ledger.json

# Harvested venue contacts — public repo, keep them out of it.
# Local copy lives at src/data/venue-contacts.json (gitignored); the CRM
# reads them from the worker (admin-authed), never from a static file.
src/data/venue-contacts.json

# Venue receipts: real counts for real businesses, kept local.
growth/receipts/
